import { VenueDetails } from "@/components/search/VenueDetails";

export default function VenueDetailsPage() {
    return (
        <div className="flex flex-col min-h-screen">
            <main className="flex-1">
                <VenueDetails />
            </main>
        </div>
    );
}
